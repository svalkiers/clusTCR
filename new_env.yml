name: test_env1
channels:
  - defaults
  - pytorch
dependencies:
  - python=3.8
  - faiss-cpu
  - pip:
      - pandas
      - networkx
      - scikit-learn
      - markov_clustering
      - pyteomics
      - tcrdist3
prefix: /home/max/anaconda3/envs/test_env
